{
  "name": "project-club",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma de comunicação em tempo real inspirada no Discord (monorepo: server + client)",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "prisma:push": "npm run prisma:push -w server",
    "deploy:start": "npm run build -w client && npm run prisma:push -w server && npm start -w server",
    "start": "npm start -w server"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "overrides": {
    "uuid": "^11.1.0"
  }
}
